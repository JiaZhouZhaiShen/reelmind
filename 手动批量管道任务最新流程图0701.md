# 手动批量管道任务全链路流程图

> 记录日期: 2026-07-01
> 对应版本: start_manual_batch 跳过已完成引擎（非 rerun 模式）

---

## 待处理数据的计数逻辑

| 引擎选择 | 待处理数的计算方式 |
|---|---|
| 只选 **场景检测** | `COUNT(DISTINCT media_id WHERE engine_name='scene' AND status='pending')` |
| 只选 **YOLO** | 同上，按 `engine_name='yolo'` 过滤 |
| 选了 **场景检测 + YOLO** | `COUNT(DISTINCT media_id WHERE engine_name IN ('scene','yolo') AND status='pending')` — 去重 |
| 选了 N 种 | 同上，engine_name IN 里加 N 个，去重 |

均通过 `GET /ai/pending-count?engines=scene,yolo` 传递，前端取 `resp.selected_pending`。

---

## 核心规则：跳过已完成，只做未做的

> 如果一个视频已经完成了场景切割和 OCR，还剩 YOLO 和 CLIP 没做，
> 批处理**只跑 YOLO 和 CLIP**，不会重置或重跑已完成的引擎。

链路：
1. `get_pending_media_ids(session, engines)` — 只查 `status='pending'` 的 job
2. 视频只要在选定引擎中**还有任何一个 pending**，就被拉入 batch
3. 发送给 AI 容器时带上引擎列表
4. AI 容器内部只处理 pending 的引擎，已 completed 的跳过

---

## 全链路流程图

```mermaid
flowchart TB
    subgraph Frontend[Web Frontend - localhost:5173]
        A[PipelineConfigPanel.tsx] -->|1. 加载配置| A1[GET /ai/pipeline/manual/config]
        A -->|2. 加载待处理数| A2[GET /ai/pending-count?engines=scene,yolo...]
        A -->|3. 切换引擎| A3[toggleEngine -> 触发 useEffect -> re-fetch pending-count]
        A -->|4. 点击 立即开始| A4[POST /ai/pipeline/manual/start]
        A -->|5. 轮询进度| A5[每 5s GET /ai/pipeline/batch/list -> 显示 block bar]
        A -->|6. 轮询引擎进度| A6[每 3s GET /ai/pipeline/batch/engine-progress/{batch_id}]
    end

    subgraph Backend[Server - FastAPI / Docker reelmind-server]
        B1[get_manual_pipeline_config] -->|读 JSON 配置| B1a[(data/configs/manual_pipeline.json)]

        B2[get_pending_asset_count] --> B2a{?engines 参数?}
        B2a -->|无参数| B2b[SELECT COUNT(DISTINCT media_id) WHERE ALL status='pending']
        B2a -->|有参数| B2c[SELECT COUNT(DISTINCT media_id) WHERE engine_name IN (scene,yolo...) AND status='pending']

        B3[start_manual_batch] --> B3a[取消全部 running 状态的旧 checkpoint]
        B3a --> B3b[get_pending_media_ids(session, engines)]
        B3b --> B3c[只筛选 status='pending' 的 media_id]
        B3c --> B3d[创建新 BatchCheckpoint -> status='running']
        B3d --> B3e[启动后台线程 _orchestrate_batch]

        B4[_orchestrate_batch] --> B4a[分 chunk: for i in range(0,total,batch_size)]
        B4a --> B4b[写入 current_chunk_ids]
        B4b --> B4c[start_pipeline -> POST AI 容器 /pipeline/start]
        B4c --> B4d[wait_for_completion -> 轮询 /pipeline/status/{task_id}]
        B4d --> B4e{成功?}
        B4e -->|是| B4f[标记 running engine jobs -> completed]
        B4e -->|否| B4g[记录 error, 继续]
        B4f --> B4h[更新 checkpoint.processed]
        B4h --> B4i[查询 chunk 内引擎完成数 -> 更新 engine_progress JSONB]
        B4i --> B4j{checkpoint 被取消?}
        B4j -->|是| B4k[break 提前结束]
        B4j -->|否| B4a
        B4a -->|全部分片完成| B4l[checkpoint.status='completed']
    end

    subgraph AI[AI Container - Docker reelmind-ai:2589]
        C1[/pipeline/start] --> C2[接收 video_ids + engines + filters]
        C2 --> C3[process_batch 内部过滤文件]
        C3 --> C4[逐个视频跑选定 engine 算法]
        C4 --> C5[调用 server API: set_job_success / set_job_error]
        C5 --> C6[返回 task 状态]
    end

    subgraph DB[(PostgreSQL - reelmind-postgres)]
        D1[(ai_engine_jobs: media_id, engine_name, status, depends_on)]
        D2[(batch_checkpoints: 全程 checkpoint)]
        D3[(pipeline_configs: auto 模式配置)]
    end

    %% 数据流连线
    B1 --> D3
    B2 --> D1
    B3b --> D1
    B3d --> D2
    B4b --> D2
    B4f --> D1
    B4h --> D2
    B4i --> D2
    B4l --> D2
    B4c -.->|HTTP POST| C1
    B4d -.->|HTTP GET| C6
    C5 -.->|HTTP| D1

    style Frontend fill:#1e293b,stroke:#64748b
    style Backend fill:#1e3a5f,stroke:#3b82f6
    style AI fill:#3b1f3b,stroke:#a855f7
    style DB fill:#1a3a2a,stroke:#22c55e
```
---

## 互斥锁机制（2026-07-01 新增）

防止手动管道和自动管道同时运行的后端互斥锁。

### 实现方式

**后端** `server/app/api/ai.py`：
- 模块级 `_orchestration_lock = threading.Lock()`
- `_orchestrate_batch()` 入口非阻塞获取锁，拿不到直接 return None
- `finally` 中释放锁
- `start_manual_batch()` 在创建 checkpoint 前先探测锁，被占用 → 返回 `{status:"error", message:"已有批处理任务正在运行..."}`
- 自动管道事件扫描器 spawn 的 `_orchestrate_batch` 同样受锁保护

**前端** `web/src/components/ai/PipelineConfigPanel.tsx`：
- 计算 `hasRunningBatch = checkpoints.some(cp => cp.status === "running")`
- 有 running checkpoint 时，按钮置灰 + 显示"已有任务运行中"
- Backend 返回 error 时显示后端消息

### 互斥范围

| 谁在运行 | 手动点击"立即开始" | 事件扫描器 spawn auto |
|---|---|---|
| 手动 batch | ❌ 后端返回 error | ❌ 锁被占，跳过 |
| 自动 batch | ❌ 后端返回 error | ❌ 锁被占，跳过 |
| 无 | ✅ 正常启动 | ✅ 正常启动 |

### 注意事项

- 锁是 Server 进程级别的（`threading.Lock`），多副本部署时不通用，但当前是单容器架构
- 已取消的 checkpoint 的正在运行 chunk 会继续跑完（AI 容器端无法被取消）
- 不会出现数据损坏，只防 GPU 争抢和重复调度
