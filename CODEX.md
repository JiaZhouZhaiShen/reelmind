# CODEX 执行约束（REELMIND）

> 本文件指导 Codex 在本项目的执行行为，是 Codex 侧的唯一约束来源。
> 铁律与规范以 `docs/铁律.md` 和 `docs/规范/` 为准，本文件是 Codex 的执行侧约束。
> 规则只作指导，不替代方案。具体问题按方案走，不在此找答案。

## 执行流程

```
备份 → 按方案执行 → 验收报告
```

1. **备份**：修改任何文件前，先备份到 `backups/{时间戳}_{描述}/`。备份完成前不允许修改。
2. **按方案执行**：只做方案指定的文件、行、改动，不自行扩大范围。
3. **验收**：完成后报告改动清单，等待验收。

## 可以做

- 按方案指定的文件和行号，执行精确修改
- 按方案要求的格式输出进度和结果
- **提出问题或发现方案中的矛盾**：如果方案描述与代码现状不符、或方案内部存在逻辑冲突、或改动可能导致预期外的副作用，应当明确指出。这不违反约束——提出观点和疑问是正确行为。

## 严禁

- **严禁** 修改方案未列出的文件
- **严禁** 修改方案未列出的行
- **严禁** 新增、删除、重排任何代码逻辑
- **严禁** 重命名方案未指定的变量、函数、类、参数
- **严禁** 修改函数签名（除非方案明确要求）
- **严禁** 修改 SQL 语句结构（除非方案明确指定改哪句）
- **严禁** 执行数据库结构变更（ALTER TABLE、DROP TABLE 等）；模型变更须由方案明确指定并走 alembic 迁移（R6.1）
- **严禁** 顺手重构、格式化、优化、整理其他代码
- **严禁** "顺便修一下" 任何不相关的 bug
- **严禁** 自行判断方案的意图并扩展改动范围
- **严禁** 在遇到错误时自行尝试不同的修复方式——应该停止并报告

## 遇到问题时的正确做法

1. 停止当前操作
2. 记录：哪个文件、哪一行、什么操作、什么错误
3. 记录：你的分析（可能的原因是什么）
4. 报告给 Claude 验收时讨论
5. **不要自己修**

## 原则

> 先备份再动手。只做方案让你做的事，不要做方案没让你做的事。
> 觉得有问题可以说，但不能自己动手改。
